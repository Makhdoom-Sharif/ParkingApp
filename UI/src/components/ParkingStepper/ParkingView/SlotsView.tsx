import Slots from "../../SlotsComponent/Slots";
import Title from "../../Title/Title";

type Props = {};

const SlotsView = (props: Props) => {
  return (
    <>
      {/* <Title titleText="Available Slots" /> */}
      <Slots />
    </>
  );
};

export default SlotsView;
